import { Subject } from "../subjects";
export interface ProductUpdatedEvent {
    subject: Subject.ProductUpdated;
    data: {
        title: string;
        description: string;
        stock: number;
        price: number;
        image: string;
        id: string;
    };
}
